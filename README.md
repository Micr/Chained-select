# Chained select with React, Redux
## Installation
```shell
 npm install
 ```
 ## Build
 ```shell
 webpack --progress --colors index.js build/bundle.js --module-bind 'js=babel-loader'
 ```
 ## Start the app
 ```shell
 npm start
```
## Running tests
```shell
npm test
```
